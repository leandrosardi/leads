<%
begin
  selected_positions = params.values.join(',')
  @login.user.preference('leads.wizard.job_positions', '', selected_positions)
  redirect "/leads/step2"
rescue => e
  redirect "/leads/step1?err="+ CGI::escape(e.message)
end
%>